apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: demo-app-summon-init
  name: demo-app-summon-init
  namespace: $APP_NAMESPACE
spec:
  selector:
    matchLabels:
      app: demo-app-summon-init
  replicas: 1
  template:
    metadata:
      labels:
        app: demo-app-summon-init
    spec:
      serviceAccountName: demo-app-summon-init-sa
      containers:
        - name: demo-app-summon-init
          image: bnasslahsen/conjur-spring-boot-demos-summon
          imagePullPolicy: IfNotPresent
          env:
            - name: CONJUR_AUTHN_TOKEN_FILE
              value: /run/conjur/access-token
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: conjur-connect
          volumeMounts:
            - mountPath: /run/conjur
              name: conjur-access-token
              readOnly: true
            - mountPath: /etc/summon
              name: summon-config
              readOnly: true
      initContainers:
        - image: registry.connect.redhat.com/cyberark/conjur-openshift-authenticator
          imagePullPolicy: IfNotPresent
          name: configurator
          volumeMounts:
            - mountPath: /run/conjur
              name: conjur-access-token
            - name: jwt-token
              mountPath: /var/run/secrets/tokens
          env:
            - name: JWT_TOKEN_PATH
              value: /var/run/secrets/tokens/jwt
            - name: CONTAINER_MODE
              value: init
            - name: MY_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          envFrom:
            - configMapRef:
                name: conjur-connect
      volumes:
        - name: jwt-token
          projected:
            sources:
              - serviceAccountToken:
                  path: jwt
                  expirationSeconds: 6000
                  audience: conjur
        - name: conjur-access-token
          emptyDir:
            medium: Memory
        - name: summon-config
          configMap:
            name: summon-config-init
            defaultMode: 420
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-summon-init
  namespace: $APP_NAMESPACE
spec:
  type: NodePort
  ports:
    - port: 8080
      nodePort: 32084
  selector:
    app: demo-app-summon-init
---
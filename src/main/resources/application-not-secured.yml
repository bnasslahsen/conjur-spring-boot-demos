spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: test
  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  writer-with-order-by-keys: true
  swagger-ui:
    use-root-path: true
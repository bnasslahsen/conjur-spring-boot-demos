spring:
  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  writer-with-order-by-keys: true
  swagger-ui:
    use-root-path: true

app:
  secret:
    url: data/bnl/ocp-apps/url
    username: data/bnl/ocp-apps/username
    password: data/bnl/ocp-apps/password


logging:
  level:
    com.cyberark: DEBUG
